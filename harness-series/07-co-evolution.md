# Harness 工程实战（七)：模型与 Harness 的协同进化——左脚踩右脚的工程哲学

---

Manus 是一个 AI Agent 项目。他们在六个月内重写了五次系统。

每次重写都删除了复杂性。

这听起来不对。按照正常逻辑，随着项目发展，系统应该越来越复杂。为什么他们的系统越写越简单？

因为模型在变强。

最初需要复杂编排才能实现的功能，新版模型直接就能做。原本需要的"管理 Agent"变成了简单的结构化交接。原本需要的复杂工具定义变成了通用 shell 执行。

![模型与 Harness 协同进化](./images/harness-co-evolution.jpg)

## 脚手架隐喻

建筑脚手架是临时基础设施，使工人能够到达他们无法触及的高度。它不做建造，但没有它，工人无法到达上层。

关键洞察：建筑完成后，脚手架会被拆除。

随着模型改进，Harness 复杂性应该降低。这是一个让人不安但无法回避的事实。

Vercel 从 v0 中删除了 80% 的工具。Anthropic 定期从 Claude Code 的 Harness 中删除规划步骤，因为新模型版本内化了该能力。

如果你的 Harness 今天需要做某件事，不代表它明天还需要做这件事。

## Claude Code 的秘密

Claude Code 的模型学会了它训练时使用的特定 Harness。

Anthropic 不是只训练了一个模型然后让它随便跑。他们训练模型时，使用了特定的工具调用模式、特定的上下文管理策略、特定的验证流程。

模型在训练中学会了"使用它训练时使用的特定 Harness"。

这带来一个奇怪的结论：改变工具实现可能会降低性能，因为这种紧密耦合。

## 协同进化原则

用更好的芯片开发下一代芯片，这是半导体行业的摩尔定律。

用更好的模型开发更好的 Harness，用更好的 Harness 开发更好的模型，这是 AI 行业的协同进化。

这不是循环，是螺旋。每一次循环，系统整体提升一个级别。

Manus 六个月重写五次的意义就在这里。他们不是在修复 bug，是在追上模型进步带来的红利。

## Harness 的未来验证测试

怎么判断一个 Harness 设计是否面向未来？

一个测试方法：用更强的模型跑相同的任务。如果性能提升了，但 Harness 复杂性没有增加，说明设计是合理的。

更严格的测试是：模型变强后，你是否能够删除某些 Harness 逻辑？

如果答案是"是"，说明你的 Harness 中确实有部分是"脚手架"，在模型更强后可以去掉。

如果答案是"否"，可能有两种情况：
1. 你的任务本身就确实需要这些逻辑
2. 你的 Harness 设计过度依赖模型的某些弱点

## 关于"被模型取代"的恐惧

作为工程师，只要你是在做最前沿的 Harness，永远也无需担心会被模型取代。

因为最好的 Harness 设计本身，就是一种对模型能力的押注。

你在赌模型往某个方向发展，然后围绕那个方向设计 Harness。如果赌对了，你的系统会因为模型进步而变强。如果赌错了，你需要重构。

这不是被取代，这是共进化。

## 动态过程

最关键的一点：Harness 工程是动态的，不是有终点的。

今天设计的 Harness，三年后可能面目全非。不是因为做错了，是因为模型变了，任务变了，整个系统都在变。

接受这个动态过程，是 Harness 工程师和传统软件工程师最大的思维差异。

传统软件工程师在设计稳定不变的系统。Harness 工程师在设计一个必须跟着模型一起演化的系统。

这更难。也更有意思。
